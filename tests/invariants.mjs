import fs from"node:fs";import assert from"node:assert/strict";const s=fs.readFileSync("contracts/AgentShadow.py","utf8");assert.match(s,/reviewer must be independent/);assert.match(s,/leader==validator/);assert.match(s,/if divergence=="DIVERGENT": return "BLOCK"/);assert.match(s,/return "HUMAN_REVIEW"/);assert.doesNotMatch(s,/py-genlayer:(test|latest)/);console.log("AgentShadow invariant checks passed: 5/5");
